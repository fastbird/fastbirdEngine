You can see .bat files in this folder.
Copy each files to each library, for example, copy _ZLIB(1.2.8)_HOME.bat to zlib folder, and execute. Then environment variable indicating the library path will be created.
This environment variables used in VS project files. If you opened VS while you are executing this batch files, you need to restart VS.